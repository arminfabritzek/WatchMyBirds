name: "WatchMyBirds CodeQL config"

queries:
  - uses: security-extended
  - uses: security-and-quality

# Sanitizer model pack lives in .github/codeql/extensions/ and is auto-discovered
# by code scanning — it must NOT be listed under packs: (that resolves names from
# ghcr.io and fails for a repo-local pack).
